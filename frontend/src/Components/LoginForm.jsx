const LoginForm = (props) => {

    const cardStyle = {
        
    }

    return (
        <div className="card" id="login-form" style={cardStyle}>
            <div className="card-body">
                <h4 className="card-title">Login</h4>
                
                
            </div>
        </div>
    );
};

export default LoginForm;
